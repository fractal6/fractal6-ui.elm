-- Do not manually edit this file, it was auto-generated by dillonkearns/elm-graphql
-- https://github.com/dillonkearns/elm-graphql


module Schema.Enum.FileHasFilter exposing (..)

import Json.Decode as Decode exposing (Decoder)


type FileHasFilter
    = CreatedBy
    | CreatedAt
    | Filename
    | ContentType
    | Size
    | StorageKey
    | Comment
    | Tension
    | User
    | Node
    | Embedded


list : List FileHasFilter
list =
    [ CreatedBy, CreatedAt, Filename, ContentType, Size, StorageKey, Comment, Tension, User, Node, Embedded ]


decoder : Decoder FileHasFilter
decoder =
    Decode.string
        |> Decode.andThen
            (\string ->
                case string of
                    "createdBy" ->
                        Decode.succeed CreatedBy

                    "createdAt" ->
                        Decode.succeed CreatedAt

                    "filename" ->
                        Decode.succeed Filename

                    "contentType" ->
                        Decode.succeed ContentType

                    "size" ->
                        Decode.succeed Size

                    "storageKey" ->
                        Decode.succeed StorageKey

                    "comment" ->
                        Decode.succeed Comment

                    "tension" ->
                        Decode.succeed Tension

                    "user" ->
                        Decode.succeed User

                    "node" ->
                        Decode.succeed Node

                    "embedded" ->
                        Decode.succeed Embedded

                    _ ->
                        Decode.fail ("Invalid FileHasFilter type, " ++ string ++ " try re-running the @dillonkearns/elm-graphql CLI ")
            )


{-| Convert from the union type representing the Enum to a string that the GraphQL server will recognize.
-}
toString : FileHasFilter -> String
toString enum____ =
    case enum____ of
        CreatedBy ->
            "createdBy"

        CreatedAt ->
            "createdAt"

        Filename ->
            "filename"

        ContentType ->
            "contentType"

        Size ->
            "size"

        StorageKey ->
            "storageKey"

        Comment ->
            "comment"

        Tension ->
            "tension"

        User ->
            "user"

        Node ->
            "node"

        Embedded ->
            "embedded"


{-| Convert from a String representation to an elm representation enum.
This is the inverse of the Enum `toString` function. So you can call `toString` and then convert back `fromString` safely.

    Swapi.Enum.Episode.NewHope
        |> Swapi.Enum.Episode.toString
        |> Swapi.Enum.Episode.fromString
        == Just NewHope

This can be useful for generating Strings to use for <select> menus to check which item was selected.

-}
fromString : String -> Maybe FileHasFilter
fromString enumString____ =
    case enumString____ of
        "createdBy" ->
            Just CreatedBy

        "createdAt" ->
            Just CreatedAt

        "filename" ->
            Just Filename

        "contentType" ->
            Just ContentType

        "size" ->
            Just Size

        "storageKey" ->
            Just StorageKey

        "comment" ->
            Just Comment

        "tension" ->
            Just Tension

        "user" ->
            Just User

        "node" ->
            Just Node

        "embedded" ->
            Just Embedded

        _ ->
            Nothing
