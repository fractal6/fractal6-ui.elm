# P3

- SW (`public/service-worker.js`): tighten path skips from `startsWith('/api'|'/auth'|'/q'|'/notifications')` to exact match or trailing-slash prefix — protects against future SPA routes silently bypassing the SW.
- Version banner (long-term): switch to backend `X-Server-Version` response header read by the GraphQL/HTTP client → free version detection on every existing request, no extra round-trips. Requires a small `fractal6.go` middleware + a way to expose response headers from `dillonkearns/elm-graphql`.

# P2

# P1

- OPT: make the role circle smaller (+ user inside + pending hint ?)
- Assign leader (instead of first-link)
- append/remove _archived suffix when archiving/unarchiving ---> *** Rename nameid function !? ***


# P0

- ModelSchema -> Schemas/Model.elm
- xr new tension dropdown refactor (sttil too long to wrote tension)


# Codefactor Elm
- as Conf.user exists now, some code function arguments could be absorb here.
- use showIf and showMaybe everywhere !
